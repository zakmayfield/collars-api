generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Agency {
    id        Int      @id @default(autoincrement())
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    name String
}

// when creating the favoriting pets model be sure to use this
// to ensure unique id combination when creating prisma schema
// @@id([userProfileId, petId])